<div class="container">
    <div class="row float-right">
       <%= link_to  "Add User", new_user_path, :class => "btn btn-primary"%>
    </div>
    <table class="table">
    <thead>
        <th>Full Name</th>
        <th>Email</th>
    </thead>
     <tbody>
        <% @users.each do |user| %>
            <tr>
                <td>
                    <%= user.full_name %>
                </td>
                <td>
                    <%= user.email %>
                </td>
                <td>
                    <%= link_to "Edit", edit_user_path(user), :class => "btn btn-primary"%>
                </td>
                <td>
                    <%= link_to "Delete", edit_user_path(user), :class => "btn btn-danger"%>
                </td>
            </tr>
            
        <% end %>
    </tbody>
    </table>
</div>